{
  "name": "mtls-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "yarn build",
    "start": "func start",
    "test": "echo \"No tests yet...\"",
    "predeploy": "yarn build",
    "deploy": "func azure functionapp publish \"mtls-api\""
  },
  "dependencies": {
    "node-forge": "^1.3.1",
    "pem": "^1.14.6",
    "sidelab-http-exceptions": "^1.0.14"
  },
  "devDependencies": {
    "@azure/functions": "^2.0.0",
    "@types/node-forge": "^1.0.4",
    "@types/pem": "^1.9.6",
    "typescript": "^4.0.0"
  }
}
